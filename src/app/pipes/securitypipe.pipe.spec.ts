import { SecuritypipePipe } from './securitypipe.pipe';

describe('SecuritypipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecuritypipePipe();
    expect(pipe).toBeTruthy();
  });
});
